git commands in order:

git init (I didn't use this one)

cd path-or-name-of-a-folder

mkdir name-of-new-folder

cd name-of-new-folder

git clone link-of-github-repository

here we make changes in code etc.
then:

git status

git add .
or
git add name-of-a-file

git status

git commit -m "message" -m "description"

git push origin main
or
git push origin master

git pull
-------------------------------------------------

git commands I haven't yet used:

git clone -b <Branch name> <Repository URL>

git pull <remote> <branch>

git log

git clone –branch <branch_name> <remote_repository_url>

git clone <repository_url> <local_folder>

git add -u (just modified and deleted files)

git rm --cached file-name (opposite of adding a file)

git reset --hard HEAD^ (exactly like this, reverts last commit)

touch file-name (creates new file)

git branch <Branch name> (To create a new git branch)
